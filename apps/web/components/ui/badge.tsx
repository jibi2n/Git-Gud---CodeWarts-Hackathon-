import { mergeProps } from "@base-ui/react/merge-props"
import { useRender } from "@base-ui/react/use-render"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

const badgeVariants = cva(
  "group/badge inline-flex h-5 w-fit shrink-0 items-center justify-center gap-1 overflow-hidden rounded-full border border-transparent px-2 py-0.5 text-xs font-semibold whitespace-nowrap transition-colors has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&>svg]:pointer-events-none [&>svg]:size-3!",
  {
    variants: {
      variant: {
        default: "bg-accent-emphasis text-fg-onEmphasis",
        secondary: "bg-bg-muted text-fg-muted border-border",
        destructive: "bg-danger-emphasis text-fg-onEmphasis",
        outline: "border-border bg-transparent text-fg",
        ghost: "border-transparent bg-transparent text-fg-muted",
        link: "border-transparent bg-transparent text-accent-fg underline-offset-4 hover:underline",
        success: "bg-success-subtle text-success-fg border-success-emphasis/30",
        attention: "bg-attention-subtle text-attention-fg border-attention-emphasis/30",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
)

function Badge({
  className,
  variant = "default",
  render,
  ...props
}: useRender.ComponentProps<"span"> & VariantProps<typeof badgeVariants>) {
  return useRender({
    defaultTagName: "span",
    props: mergeProps<"span">(
      {
        className: cn(badgeVariants({ variant }), className),
      },
      props
    ),
    render,
    state: {
      slot: "badge",
      variant,
    },
  })
}

export { Badge, badgeVariants }
