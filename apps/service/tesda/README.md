# tesda/

**Owner:** Role 2 (CV + Data Pipeline Engineer)

TESDA competency standards data and skill-to-certification mapping rules. Welding first (MDP scope; see SDD §4.4).

Add certification data as JSON files (e.g. `welding.json`). Mapping rules live in Python modules alongside.
